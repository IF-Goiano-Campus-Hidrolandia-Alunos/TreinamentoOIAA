# Copie para .env.local (dev) e configure no painel da Vercel (Production).

# Token exigido nas escritas do placar (POST/DELETE /api/leaderboard).
# Gere um valor forte, ex.: openssl rand -base64 24
ADMIN_TOKEN=
