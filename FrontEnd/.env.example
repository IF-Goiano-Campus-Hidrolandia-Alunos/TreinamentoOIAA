# Variaveis do FrontEnd (Vite). Defina na Vercel (projeto do FrontEnd), em Production.

# URL do BackEnd (projeto Vercel separado que expoe /api/teams e /api/scores).
# Ex.: https://oiaa-backend.vercel.app   (sem barra no final)
# Vazio = mesma origem. Se a API nao responder, o app usa o MODO LOCAL
# (seed + localStorage por navegador). Com a URL correta, entra no MODO API
# (placar compartilhado e persistente no Neon).
VITE_API_URL=
